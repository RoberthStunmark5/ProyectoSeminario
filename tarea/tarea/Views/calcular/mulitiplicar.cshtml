@model tarea.Models.calculo

@{
    ViewBag.Title = "mulitiplicar";
}

<h2>mulitiplicar</h2>
@using(@Html.BeginForm())
{
    @Html.TextBoxFor(s => s.a);
    @Html.TextBoxFor(s => s.b);
    <input type = "submit" value="calcular" />
}
@ViewBag.resultado2